/**
 * Enum defining user roles.
 */
export enum Role {
  USER = 'USER',
  ADMIN = 'ADMIN',
  SUPER_ADMIN = 'SUPER_ADMIN',
}

/**
 * Enum defining permissions for user roles.
 */
export enum Permissions {
  READ = 'READ',
  WRITE = 'WRITE',
  EDIT = 'EDIT',
}
